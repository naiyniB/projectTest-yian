---
import { type TProductCategory } from "@rxdrag/website-lib-core";
import { Link } from "@rxdrag/website-lib";

interface Props {
  category?: TProductCategory;
}
const { category } = Astro.props;---

<div>
  <Link
    class:list={[
      "block p-2 font-semibold text-gray-900 leading-6 rounded-lg actived:text-sky-600 disabled:opacity-50",
      "hover:bg-gray-50 hover:text-sky-600",
      "menu-item",
    ]}
    type="product-category"
    to={category?.slug}
  >
    {category ? category?.name : "no category"}
  </Link>
  <div>
    {
      category?.products?.map((product) => {
        return (
          <Link
            class:list={[
              "w-full rounded-md flex items-center gap-x-2 text-sm text-gray-500 p-2 hover:bg-gray-50 hover:text-sky-600",
              "menu-item",
            ]}
            type="product"
            to={product.slug}
          >
            {product.title}
          </Link>
        );
      })
    }
  </div>
</div>
